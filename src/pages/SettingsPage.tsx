import { Typography } from "@mui/material";

export default function SettingsPage() {
  return (
    <div className="p-6">
      <Typography variant="h4" className="font-bold mb-4">
        设置
      </Typography>
      <Typography>这里将放置设置选项...</Typography>
    </div>
  );
}
